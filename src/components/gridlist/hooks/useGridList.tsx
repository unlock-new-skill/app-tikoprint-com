import { GridListProps } from '../gridlist.dto'

export default function useGridList<DataDto, FilterQueryDto>(
	props: GridListProps<DataDto, FilterQueryDto>
) {
	console.log('🚀 ~ props:', props)
	// const {apiService, gridClassName, renderCard} = props

	return <div>4144</div>
}
